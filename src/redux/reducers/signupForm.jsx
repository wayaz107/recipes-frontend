

export default function manageSignupForm(state, action) {
    switch (action.type) {
        case "UPDATE_SIGNUP_FORM":
        case "RESET_SIGNUP_FORM":
        default:
            return state
    }
}